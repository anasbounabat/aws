# www-admin (frontend)

Frontend admin séparé (separate build) – Tailwind.

Fonctionnalités:
- Login
- Dashboard stats globales
- Liste des users (via Cognito)
- Liste des backups

TODO:
- créer l'app
- intégrer Cognito + rôle admin
- consommer l'API REST Hono (routes `/admin/*`)

